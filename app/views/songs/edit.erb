<!DOCTYPE html>
<html>
<head>
	<title>Update</title>
</head>
<body>
<h1>Update Song</h1>
	<form action="/songs/<%=@song.slug%>" method=POST>
		<label>Name:</label>
		<input type="text" name="song[name]" value="<%=@song.name%>" id="Name">

		<br></br>

		<label>Artist:</label>
		<input type="text" name="song[artist][name]" value="<%=@song.artist.name%>" id="Artist Name">

		<br></br>

		<label>Genre:</label>
		<br>
		<%Genre.all.each do |g|%>
			<input type="checkbox" name="song[genre_ids][]" value="<%=g.id%>" id="<%=g.name%>" <%='checked' if @song.genres.include?(g)%>><%=g.name%></input>
		<%end%>

		<br></br>

		<input type="submit" value="Save">
	</form>

</body>
</html>